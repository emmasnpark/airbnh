<div class="row">
  <div class="col-md-3">
    <p><%= link_to "Your Houses", rooms_path %></p>
    <p><%= link_to "Your Trips", your_trips_path %></p>
    <p><%= link_to "Your Reservations", your_reservations_path %></p>
  </div>
  <div class="col-md-9">
    <h1 class="my_history">My Houses</h1>
    <div class="panel panel-primary">
      <div class="panel-heading">
        Listings
      </div>
      <div class="panel-body">

        <% @rooms.each do |room| %>
          <div class="row">
            <div class="col-md-2">
              <%= link_to room_path(room) do %>
                <%= image_tag room.cover_photo(:thumb) %>
              <% end %>
            </div>
            <div class="col-md-6">
              <%= link_to room_path(room) do %>
                <h4><%= room.listing_name %></h4>
              <% end %>
            </div>

            <div class="col-md-2 right">
              <%= link_to "Update", listing_room_path(room), class: "btn btn-form" %>
            </div>
            <div class="col-md-2 right">
              <%= link_to "Show", room_path(room), class: "btn btn-form" %>
            </div>
          </div>
          <hr/>
        <% end %>
      </div>
    </div>
  </div>
</div>
